# xll-gen build + generated artifacts (regenerate with `xll-gen generate`).
build/
generated/
temp_*/
.vscode/

# Go server binaries
*.exe
*.xll

# Logs emitted next to the .xll (logging.dir = ${XLL_DIR})
*.log

# Vendored Go modules (archived in vendor.tar.gz)
vendor/
vendor.tar.gz
